# 发布证据

## V0.0.7 - 2026-08-30

### 范围

修复 V0.0.6 在 NFS 上恢复已发布但未记账录像时，把额外硬链接误判为不安全的问题。
本版本只改变已存在普通文件的只读恢复判定；恢复仍执行大小与媒体探测，符号链接仍拒绝，
保留删除仍要求单链接并且只处理状态中已登记文件。云认证、下载、命名、状态日志和删除
职责不变。

### 当前验证门

- [x] V0.0.6 前代安装包通过 SHA-256、内部版本、17 文件安全完整解压和恢复路径预检。
- [x] Windows 全量 46/46 单元与合成媒体测试通过；新增额外硬链接恢复与符号链接拒绝。
- [x] 固定 Home Assistant `2026.8.2` 禁网容器完成生命周期和 46/46 测试；任务容器
  自动删除且既有非目标容器身份保持不变。
- [x] 公开树审计通过：44 个候选文件、17 条规则。
- [ ] Pull Request 与公开 CI。
- [ ] V0.0.7 安装包、哈希旁车、受限解压、manifest 与源码一致性。
- [ ] 目标 HAOS 部署、配置检查、重启后健康和集成版本复核。
- [ ] 中断文件补记、剩余下载、最终 dry-run、脱敏状态与识别消费者验收。

V0.0.6 活动目录、安装包和完整 HA 备份在 V0.0.7 完成目标及消费者验收前不得删除。

## V0.0.6 - 2026-08-30

### 范围

修复 V0.0.5 在目标 Home Assistant 前端的零值兼容：前端将必填数字 `0` 判为空值，
导致提交在到达后端前被拒绝。本版本只把保留天数表单改为必填数字文本；提交后仍由
统一运行时校验限制 `0..3650` 并归一化为整数。云调用、下载、状态日志、媒体命名、
认证和删除边界不变。

### 当前验证门

- [x] V0.0.5 前代安装包通过 SHA-256、内部版本、17 文件安全完整解压和恢复路径预检。
- [x] Windows 全量 44/44 单元与合成媒体测试通过。
- [x] 固定 Home Assistant `2026.8.2` 禁网容器完成生命周期、字符串零值与后端整数
  归一化断言和 44/44 测试；任务容器自动删除且非目标容器身份保持不变。
- [x] 公开树审计通过：44 个候选文件、17 条规则。
- [x] Pull Request `#13`、公开 CI 与合并后同步。
- [x] V0.0.6 安装包、哈希旁车、17 文件受限解压、manifest 与源码一致性。
- [x] 目标 HAOS 完整备份、部署、保留天数 `0` 复读、配置检查与重启后健康。
- [ ] 一次正常运行已产生 6 条有效普通文件，但与 Core 重启重叠而未完成状态保存；后续
  NFS 恢复误判转入 V0.0.7，消费者验收尚未完成。

V0.0.5、V0.0.4、V0.0.3 安装目录、安装包和目标完整备份在 V0.0.6 完成目标及消费者
验收前不得删除。

## V0.0.5 - 2026-08-30

### 范围

修复 V0.0.4 的保留天数表单与运行时规则漂移：运行时已接受 `0`，首次配置与选项表单
仍限制至少 1，导致页面看似提交后目标值没有持久保存。本版本只统一两个表单的范围，
不改变云调用、下载、状态日志、媒体命名或认证边界。

### 当前验证门

- [x] Windows 全量 44/44 单元与合成媒体测试通过。
- [x] 固定 Home Assistant `2026.8.2` 禁网容器完成生命周期、双表单零值断言和
  44/44 测试；任务容器均使用 `--rm`，结束后无残留。
- [x] 公开树审计通过：44 个候选文件、17 条规则；完整差异无空白错误。
- [x] Pull Request `#12`、公开 CI 与合并后同步。
- [x] V0.0.5 安装包、哈希旁车、17 文件受限解压、manifest 与源码一致性。
- [x] 目标 HAOS 完整备份、部署、配置检查、重启后健康和集成加载；目标前端拒绝
  必填数字 `0`，选项复读仍为 30，因此零值持久化门未通过。
- [ ] 一次正常运行、脱敏状态消费者读取和源媒体不变验证。

V0.0.4 已在目标完成新完整备份、安装、配置检查、Core 重启和版本加载；因选项复读仍
为 30，未执行正常运行。V0.0.5 活动目录、V0.0.4 与 V0.0.3 回滚目录和完整备份在
V0.0.6 完成目标验收前不得删除。

### V0.0.5 产物

| 产物 | SHA-256 | 文件数 | 结论 |
| --- | --- | --- | --- |
| `artifacts/V0.0.5/xiaomi-lock-cloud-video-backup-V0.0.5.zip` | `2384BD5953A23EFA516A31149FD6282539F49B8251FF2CE8EF968BB64F95D2EF` | 17 | 受限解压、manifest、源码一致性通过 |

## V0.0.4 - 2026-08-29

### 范围

新增脱敏本地状态日志，让独立 Doorlock Sentinel 在不读取小米、米家或 Home Assistant
认证信息的前提下获得下载成功、重试和最终失败状态；同时允许保留天数 `0`，把本地
删除职责完整交给远端校验后的 115 备份专项任务。人脸识别不进入本集成。

### 当前验证门

- [x] Windows 全量 44/44 单元与合成媒体测试通过。
- [x] 固定 Home Assistant `2026.8.2` 禁网容器完成生命周期、正常零事件运行、
  脱敏状态日志和 44/44 测试。
- [x] 公开树审计通过：43 个候选文件、17 条规则；完整差异无空白错误。
- [ ] V0.0.3 前代包及目标完整备份恢复路径复核；本地前代包哈希已再次通过，目标完整
  备份仍需在部署前重新读取确认。
- [x] V0.0.4 安装包与哈希旁车通过；受限解压后 17 文件、manifest `0.0.4`，
  与当前组件源码逐文件一致。
- [x] GitHub Pull Request `#10`、公开 CI 与合并后本地同步；合并提交为
  `d46d22617f1b6329bb28a27b2fd38189210b907a`。
- [ ] 目标 HAOS 部署、保留天数 `0`、配置检查、重启后健康和集成加载。
- [ ] 一次正常运行、状态日志去标识、识别消费者读取和源媒体不变验证。

V0.0.4 完成上述门前保持 `PENDING`。V0.0.3 是当前已验证运行代，不得提前删除其安装
目录、封存包或完整 Home Assistant 备份。

### V0.0.4 候选产物

| 产物 | SHA-256 | 文件数 | 结论 |
| --- | --- | --- | --- |
| `artifacts/V0.0.4/xiaomi-lock-cloud-video-backup-V0.0.4.zip` | `8D16D9F94042E57927BBC648690194A3A5C0878C51C4AFCB2CB773040A74E774` | 17 | 受限解压、manifest、源码一致性通过 |

构建脚本拒绝覆盖既有版本目录，并从 Git 候选树选取组件文件，避免把 `__pycache__`、
测试媒体或本地运行状态装入发布包。

## V0.0.3 - 2026-08-28

### 范围

新增独立、显式和可恢复的历史回填服务。云端空事件页只有在继续标记结束时才视为
历史终点；继续游标必须严格向过去移动。单页未全部成功或隔离时不提交下一页游标，
旧录像成功不会倒退每日增量游标。服务响应和诊断只增加固定状态、计数与布尔值。

### 当前验证门

- [x] Windows 主机 37/37 测试通过。
- [x] 固定 Home Assistant `2026.8.2` 镜像在禁网容器中完成双服务注册、历史只读调用、
  诊断、定时器与卸载生命周期验证，同时 37/37 测试通过。
- [x] V0.0.2 前代 ZIP 完成哈希旁车比对、受限解压、16 文件和 manifest `0.0.2`
  验证，可作为安装回退源。
- [x] 公开树审计通过：40 个候选文件、17 条规则；完整差异无空白错误。
- [x] V0.0.3 安装包完成哈希旁车、受限解压、16 文件、manifest `0.0.3` 和当前
  组件树逐文件一致性验证。
- [x] GitHub Pull Request `#7`、公开 CI 与合并后同步；合并提交为
  `9cff80296e8b82e085847788be4840be02c22e19`。
- [x] 目标 HAOS 完整备份、V0.0.3 部署、历史只读预检、正常回填和最终零待处理复核。
- [x] 全部目标 MP4 的视频流、正时长、临时文件与目标存储一致性复核。

### 目标环境验收

- 执行前创建并验证完整 HA 备份，确认数据库、Home Assistant 配置、附加组件与文件夹
  均纳入备份；备份标识不进入公开记录。
- V0.0.3 候选来自不可变合并提交，16 文件安装树摘要为
  `818a38054d67e1c24082cd5e7aa45f2fb28ca2785091177d7c9a68b3a4de828a`，目标安装树
  与候选一致。旧版本目录移出 `custom_components` 后，该域只保留一个活动目录。
- `ha core check` 通过；重启后 Core `2026.8.2` 健康，Web UI 可用，集成显示
  manifest `0.0.3`，`run_backup` 与 `run_history_backfill` 均已注册。
- 历史 dry-run 扫描 2 页、可用 7、选择 7；正常调用返回 `history_complete`，下载 7、
  恢复 0、失败 0、隔离 0、删除 0。再次历史 dry-run 返回 0 页、0 可用、0 选择，
  每日增量 dry-run 返回 `dry_run_ok`、0 可用、0 选择。
- NAS 目标由 5 条增加到 12 条；12/12 均通过视频流与正时长探测，临时文件和符号链接
  均为 0。真实事件时间、文件名、摘要、媒体内容与挂载标识未写入公开记录。

该证据证明本次目标部署、当前云端可查询历史终点和当前媒体消费路径；首次每日计划
周期、长期稳定性、断网重试、空间不足和保留删除仍为 `UNKNOWN`。V0.0.2 可运行前代、
V0.0.1 第二恢复集和完整 HA 备份继续保留。

### 安装包 FINAL_PASS

| 版本 | 本地路径 | SHA-256 | 内部版本 | 受限解压 | 源码一致性 |
| --- | --- | --- | --- | --- | --- |
| V0.0.2 | `artifacts/V0.0.2/xiaomi-lock-cloud-video-backup-V0.0.2.zip` | `87DB5C918A646D397E571D7746AF38680946F2763A1252C8FB47B1AB3EC6CCC3` | `0.0.2` | 16 文件，通过 | 前代封存包，不与 V0.0.3 源码比较 |
| V0.0.3 | `artifacts/V0.0.3/xiaomi-lock-cloud-video-backup-V0.0.3.zip` | `D1241B78B2A012B00FB7939C0F6F2E89C195F17DA2F64E28552A3BC96BDFC171` | `0.0.3` | 16 文件，通过 | 与当前组件树逐文件相同 |

V0.0.3 后续运行异常时，先停止新运行并恢复完整 HA 备份或 V0.0.2 安装目录/封存包；
恢复验收要求 manifest 回到 `0.0.2`、配置检查通过、Core 重启后健康且既有媒体数量与
解析结果不退化。V0.0.2、V0.0.1 和现有目标媒体在首次每日计划周期通过前继续保留。

## V0.0.2 - 2026-08-28

### 范围

本版本修复目标设备发现兼容性：对多个已加载会话中的同一物理设备去重；云设备列表
没有匹配时，仅从 Xiaomi Miot 已加载实体索引恢复绑定，并继续要求使用已加载的内存
云会话。零目标与多个不同目标使用不同的固定错误码，均不返回设备编号。

### 已完成验证门

- [x] Windows 主机 29/29 测试通过。
- [x] 公开树审计通过：39 个候选文件、17 条规则。
- [x] 固定 Home Assistant `2026.8.2` 镜像在禁网容器中完成内存实体回退、服务调用、
  诊断版本、定时器与卸载生命周期验证，同时 29/29 测试通过。
- [x] GitHub Pull Request `#4` 的公开 CI 通过并完成合并；合并后本地
  `main == origin/main`，合并提交为
  `bb95ac0cf3e321055557b4c8e6c4c29caf77da80`。
- [x] 生成并验证 `V0.0.2` 安装包与 SHA-256。
- [x] 真实 Home Assistant 目标完成升级、配置检查、单次重启和只读复核；媒体目录保持
  不存在。

### 后续目标验证

- [x] 在单独授权的目标环境中以单条上限调用一次正常 `run_backup`；固定结果为
  `limit_reached`，发现 3 条、选择 1 条、下载 1 条、失败 0、隔离 0、删除 0。
- [x] 新样本在 Home Assistant 的 NAS 媒体消费路径中通过 `ffprobe`，包含视频轨和
  音频轨；未记录或提交文件名、摘要、时间戳或媒体内容。
- [ ] 观察至少一个每日计划周期，并验证断网重试、空间不足和保留删除；当前保持
  `UNKNOWN`。

### 目标环境证据

- 目标为 HAOS `18.2`、Home Assistant Core `2026.8.2`。
- 切换前确认已安装 manifest 为 `0.0.1`；新建完整 HA 备份，并保留可读的 27 文件
  V0.0.1 安装目录归档和原安装目录。
- 目标端候选 ZIP 的 SHA-256 与本地产物相同；解压后 16 个组件文件与候选树逐文件
  相同。
- `ha core check` 成功；单次重启命令成功，随后 `ha core info` 返回 `ok` 和
  `2026.8.2`，伴侣工作台报告 Core `running`，HTTP 首页返回 `200`。
- 重启后安装目录为 `0.0.2` 且仍与候选逐文件相同；V0.0.1 原安装目录保持为
  `0.0.1`。最近 500 行 Core 日志中本组件错误计数和 Xiaomi Miot 错误计数均为 0。
- Home Assistant 媒体目录已切换到 Supervisor 管理的 NFS 媒体挂载，后端位于 NAS
  NVMe 存储；Core 再次启动后挂载状态仍为活动。
- 一条既有本地样本先迁移到 NAS，并完成等大小与摘要一致性核验；正常服务调用随后
  新增一条样本。Home Assistant 消费路径与 NAS 目标均看到两条 MP4，临时文件为 0。
- 新样本经 `ffprobe` 验证为一条视频轨、一条音频轨且时长与大小均为正。证据只保留
  固定结果码、计数和布尔结论，不保留媒体标识、事件时间或摘要值。
- 测试使用的临时 Home Assistant 账号、登录凭据和刷新会话已从活动认证存储移除；
  仓库没有保存密码、Cookie、令牌或米家认证值。
- 当前近期日志窗口中，本组件、Xiaomi Miot 和挂载错误条目均为 0。更早的启动阶段
  曾出现 Xiaomi Miot 错误记录，但真实下载和后续近期窗口已经通过；该历史记录不应
  被改写为长期稳定性证据。

### V0.0.2 产物

- `xiaomi-lock-cloud-video-backup-V0.0.2.zip`：16 个文件，manifest `0.0.2`，
  SHA-256 `87DB5C918A646D397E571D7746AF38680946F2763A1252C8FB47B1AB3EC6CCC3`。
- 安装包与 `SHA256SUMS.txt` 保留在项目本地 `artifacts/V0.0.2`，不进入公开 Git 树。

### 前代恢复基线

- `xiaomi-lock-cloud-video-backup-V0.0.1.zip`：16 个文件，manifest `0.0.1`，
  SHA-256 `799E9B744EAD5B14587FC802ED59124D19472990D1DC3D717435194C1C013624`。
- 该安装包保留在项目本地 `artifacts/V0.0.1`，不进入公开 Git 树；真实 dry-run 和
  至少一个消费路径验证前不得移除。消费路径现已验证，但在首个每日计划周期通过前
  仍继续保留该回滚集。

## V0.0.1 - 2026-08-28

## 范围

本版本交付公开源码、Home Assistant 自定义集成、无真实云调用的测试、隐私审计和
回退说明。不包含真实 Home Assistant 安装、NAS 挂载、真实录像下载或人脸识别。

## 验证门

- [x] 本地单元与合成媒体测试全部通过。
- [x] 公开树隐私审计通过。
- [x] 固定 Home Assistant `2026.8.2` 镜像在无网络容器中成功导入全部模块。
- [x] 完整 Git 差异无空白错误。
- [x] GitHub Pull Request 检查通过并合并到公开 `main`。
- [x] 本地 `main` 与 `origin/main` 相同且工作区干净。

## 2026-08-28 验证结果

- Windows 主机：23/23 测试通过；包含合成 AES-128 HLS、音轨保留、MP4 验证、
  状态去标识、分页、路径约束、符号链接和硬链接拒绝。
- Home Assistant：镜像 `2026.8.2`，固定摘要
  `sha256:6e8225ea9de2cfe9292b634e554ebbf439118ca0c823221d794298e7a74404bb`；
  禁网容器中 manifest 加载、配置项初始化、服务注册、定时器和卸载生命周期通过，
  只读服务调用通过，同时 23/23 测试通过。
- 公开树审计：39 个候选文件、17 条规则，通过；未包含媒体、运行状态、私网地址、
  本地主机名、机器路径或凭据字面量。
- GitHub：Pull Request `#1` 的公开 CI 通过并完成合并；源码实现合并提交为
  `39fda37e5e70b7b9d5e773a7825ee6e1f0430220`。
- `LICENSE` SHA-256：
  `C71D239DF91726FC519C6EB72D318EC65820627232B2F796219E87DCF35D0AB4`。

## 产物与恢复

- 源码产物：GitHub `main` 分支的合并提交。
- 许可证：Apache-2.0；本地 `LICENSE` 在发布前记录 SHA-256。
- 恢复：删除或禁用配置项以停止计划；移除自定义集成目录并重启。卸载不会删除媒体。

真实设备、云端媒体兼容性、目标存储连续性和业务可用性在目标环境验证前均为
`UNKNOWN`。
