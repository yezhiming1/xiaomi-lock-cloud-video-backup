# 待办与未决项

## V0.0.8 执行中

- 已完成事件触发、120 秒尾沿防抖、北京时间文件名、同秒无覆盖和显式历史改名服务的
  实现；纯 Python 51 项回归与公开树审计通过。
- 固定 HA 容器验证因测试镜像拉取未完成而中止并清理临时容器/目录；不得据此宣称
  HA 生命周期通过，改由目标配置检查、重启加载、服务注册和诊断复读补齐。
- 待完成：发布包与公开 CI、目标 HA 完整备份、V0.0.8 部署、两个私有事件实体配置、
  历史录像 dry-run/应用/摘要复核，以及 Doorlock Sentinel 配套图片迁移。
- 115 不在本次改名链中；任何远端同名对象与回执保持不变。

## V0.0.7 已完成

- Pull Request、公开 CI、安装包与哈希验证完成。
- 目标部署、保留天数 `0`、配置检查、Core 重启、版本与动作注册完成。
- 单次正常运行下载 10、恢复 1、失败 0；状态为 `ok`，最终 dry-run 为 0 待处理。
- Doorlock Sentinel 已只读消费 29 条受管录像，分析错误为 0；源目录无临时文件或
  符号链接。V0.0.6 及完整 Home Assistant 备份继续保留为首选回滚集。

## 已完成的目标验证

- 已在单独授权下以单条上限完成一次正常 `run_backup`：发现 3 条、选择 1 条、下载
  1 条、失败 0、隔离 0、删除 0；未保留私有标识。
- 已把 Home Assistant 媒体目录切换到 NAS NVMe 后端的 NFS 挂载，并验证 Core
  重启后挂载仍为活动。
- 已确认 Home Assistant 消费路径与 NAS 目标文件数量和大小一致、没有临时文件；
  新样本通过音视频解析。
- 已创建并验证完整 HA 备份，部署唯一的 V0.0.3 活动安装目录；配置检查、重启后
  Core 健康、集成加载与双服务注册均通过。
- 已完成真实历史回填：下载 7 条后云端明确返回完成，最终历史与每日增量 dry-run 均
  为零待处理；NAS 目标共 12 条，12/12 通过视频流与正时长检查，临时文件和符号链接
  均为 0。

## 仍待验证

1. 观察至少一次真实“经过/停留”事件，确认最后一次变化 120 秒后触发、游标推进、
   新命名和识别消费；03:30 对账与长期稳定性在各自实际周期通过前保持 `UNKNOWN`。
2. 在受控条件下验证 NFS 断开重试、空间不足和保留删除；不得影响现有媒体或其他 NAS
   业务。
3. 115 网盘副本、月度封存、恢复和校验后删除由独立专项任务负责；本集成只通过保留
   天数 `0` 放弃删除权，不宣称外部备份完成。
4. 评估其他门锁型号；不得因型号字符串相似就宣称兼容。
5. 按更多真实样本证据决定是否支持 master playlist、`EXT-X-MAP` 或其他加密格式。
6. 陌生人识别由独立 Doorlock Sentinel 项目实现；不得把其模型、人脸、数据库、管理
   页面或企业微信凭据并入本集成。
