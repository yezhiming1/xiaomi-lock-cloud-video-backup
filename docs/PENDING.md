# 待办与未决项

1. 在用户自有设备上先执行只读预检，再下载一条无敏感内容的真实样本；记录时只保留
   固定结果码和计数，不提交录像或私有标识。
2. 在已有的 Home Assistant 网络媒体挂载上验证重启、断网重试、空间不足与连续每日
   计划；没有目标环境证据前保持 `UNKNOWN`。
3. 评估其他门锁型号；不得因型号字符串相似就宣称兼容。
4. 按真实样本证据决定是否支持 master playlist、`EXT-X-MAP` 或其他加密格式。
5. 陌生人识别若进入后续版本，应采用独立、默认关闭的本地处理边界，并另行解决告知、
   保留期限、误报和生物特征隐私问题。
