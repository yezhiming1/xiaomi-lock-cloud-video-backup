# 待办与未决项

## V0.0.4 发布与部署

1. 完成全量测试、固定 HA 容器、公开树审计和 V0.0.4 安装包验证。
2. 通过 GitHub Pull Request 合并，不直接把候选分支推成公开主线。
3. 部署前确认完整 HA 备份和 V0.0.3 安装目录/封存包仍可恢复。
4. 在目标配置项把保留天数改为 `0`，验证下载器不再执行删除。
5. 正常运行一次并从目标重新读取状态日志；只核对 schema、状态、次数、固定错误码和
   摘要格式，不输出报告键、真实时间或媒体标识。
6. 验证 Doorlock Sentinel 只读消费状态后，源录像目录数量、大小和摘要不变。

## 已完成的目标验证

- 已在单独授权下以单条上限完成一次正常 `run_backup`：发现 3 条、选择 1 条、下载
  1 条、失败 0、隔离 0、删除 0；未保留私有标识。
- 已把 Home Assistant 媒体目录切换到 NAS NVMe 后端的 NFS 挂载，并验证 Core
  重启后挂载仍为活动。
- 已确认 Home Assistant 消费路径与 NAS 目标文件数量和大小一致、没有临时文件；
  新样本通过音视频解析。
- 已创建并验证完整 HA 备份，部署唯一的 V0.0.3 活动安装目录；配置检查、重启后
  Core 健康、集成加载与双服务注册均通过。
- 已完成真实历史回填：下载 7 条后云端明确返回完成，最终历史与每日增量 dry-run 均
  为零待处理；NAS 目标共 12 条，12/12 通过视频流与正时长检查，临时文件和符号链接
  均为 0。

## 仍待验证

1. 观察至少一个每日计划周期，确认计划触发、游标推进、幂等跳过和异常通知；完成前
   长期自动备份保持 `UNKNOWN`。
2. 在受控条件下验证 NFS 断开重试、空间不足和保留删除；不得影响现有媒体或其他 NAS
   业务。
3. 115 网盘副本、月度封存、恢复和校验后删除由独立专项任务负责；本集成只通过保留
   天数 `0` 放弃删除权，不宣称外部备份完成。
4. 评估其他门锁型号；不得因型号字符串相似就宣称兼容。
5. 按更多真实样本证据决定是否支持 master playlist、`EXT-X-MAP` 或其他加密格式。
6. 陌生人识别由独立 Doorlock Sentinel 项目实现；不得把其模型、人脸、数据库、管理
   页面或企业微信凭据并入本集成。
