# 当前状态

- 项目版本：`V0.0.7`
- 集成域：`xiaomi_lock_cloud_backup`
- 发布级别：V0.0.7 已由公开 Pull Request 合并，安装包完成哈希、受限解压和源码一致性
  验证，并已部署到目标 Home Assistant；保留天数 `0`、配置检查、Core 重启、动作注册、
  单次正常运行和最终只读复核均通过，发布证据见 `RELEASE.md`
- 目标型号：默认 `xiaomi.lock.s1`
- 数据边界：只复用已加载的 `hass-xiaomi-miot` 内存会话
- 历史回填：目标真实调用已到达云端明确终点，最终只读复核为零待处理
- 默认计划：Home Assistant 本地时间每日 `03:30:00`
- 默认保留：30 天；目标 V0.0.7 已把 `0` 持久化，后端完全禁用下载器删除
- 陌生人识别：不在本版本范围内

V0.0.4 为独立门锁录像识别/运维消费者追加脱敏 JSONL 状态交接：每次正常非 dry-run
只写固定状态、固定错误码、尝试次数、UTC 记录时间和不可逆 SHA-256 报告键。前两次
连续基础设施失败为 `retrying`，第三次为 `failed`，成功运行清零。日志不包含设备、
录像、账号、URL、凭据或媒体标识。目标部署将保留天数设为 `0`，由 115 备份专项任务
在远端校验后独占删除职责。

`V0.0.3` 保留 V0.0.2 的目标发现：先合并云设备列表中的重复设备；列表没有匹配时，仅从
Xiaomi Miot 已加载的内存实体索引恢复目标，且云对象仍必须来自已加载会话。

历史回填不会自动运行。它要求云端明确返回继续/完成标记，空白页面可继续向更早时间
扫描，游标必须严格递减；单页未全部处理时不会跳过。每次最多扫描 20 页、选择 100 条，
可重复执行至 `history_complete=true`。每日增量游标保持独立。

本地纯测试、固定 Home Assistant 镜像和目标 HAOS 证据记录在 `RELEASE.md`。目标执行前
已创建并验证完整 HA 备份，保留 V0.0.2 可运行前代；V0.0.3 的 16 文件安装树与候选树
一致，配置检查、重启后 Core 健康、集成加载和双服务注册均通过。

历史只读预检扫描 2 页并选择 7 条；正常调用随后返回 `history_complete`，下载 7 条、
失败 0、隔离 0、删除 0。最终历史只读复核为 0 页、0 可用、0 选择，每日增量只读复核
也为 0 可用、0 选择。NAS 目标共有 12 条 MP4，12/12 均含视频流且时长为正，临时文件
和符号链接均为 0。首次每日计划周期、断网重试、空间不足和保留删除仍未验证，保持
`UNKNOWN`；115 网盘副本尚未配置。

上述下载与媒体目标证据属于 V0.0.3。V0.0.4 已完成公开发布和目标安装，并通过新完整
备份、配置检查、Core 重启、集成版本和近期组件错误计数复核；目标选项仍复读为 30，
源码确认两个表单错误保留 `min=1`，所以没有执行可能触发旧保留策略的正常运行。
V0.0.5 虽统一了后端和表单范围，但目标前端仍拒绝必填数字 `0`。V0.0.6 调整该字段
的前端表示并在目标成功保存 `0`。V0.0.7 只放宽已存在普通文件的只读恢复，不放宽
符号链接或删除边界。目标正常运行返回下载 10、恢复 1、失败 0，脱敏状态为 `ok`、
连续失败 0、固定错误码为空；随后 dry-run 为 0 可用、0 选择。只读识别消费者已处理
29 条受管录像且分析错误为 0，源目录保持 29 条非空普通文件、无临时文件或符号链接。
首次每日计划周期、断网、空间不足和长期稳定性仍为 `UNKNOWN`。
