# 当前状态

- 项目版本：`V0.0.2`
- 集成域：`xiaomi_lock_cloud_backup`
- 发布级别：源码已由 Pull Request `#4` 合并；目标 HAOS 已升级并通过配置检查、单次
  重启和运行复核；安装包及哈希已封存
- 目标型号：默认 `xiaomi.lock.s1`
- 数据边界：只复用已加载的 `hass-xiaomi-miot` 内存会话
- 历史回填：关闭；首次配置从当前时间建立游标
- 默认计划：Home Assistant 本地时间每日 `03:30:00`
- 默认保留：30 天
- 陌生人识别：不在本版本范围内

`V0.0.2` 的目标发现先合并云设备列表中的重复设备；列表没有匹配时，仅从
Xiaomi Miot 已加载的内存实体索引恢复目标，且云对象仍必须来自已加载会话。

本地纯测试、固定 Home Assistant 镜像和目标 HAOS 证据记录在 `RELEASE.md`。真实服务
`dry_run` 因无可用 HA 登录会话保持 `UNKNOWN`；真实账号媒体下载、NAS 持久化和长期
定时运行在各自目标环境验证前也保持 `UNKNOWN`。
