# 当前状态

- 项目版本：`V0.0.2`
- 集成域：`xiaomi_lock_cloud_backup`
- 发布级别：源码已由 Pull Request `#4` 合并；目标 HAOS 已升级，并通过真实单条下载、
  NAS 媒体挂载和重启后复核；安装包及哈希已封存
- 目标型号：默认 `xiaomi.lock.s1`
- 数据边界：只复用已加载的 `hass-xiaomi-miot` 内存会话
- 历史回填：关闭；首次配置从当前时间建立游标
- 默认计划：Home Assistant 本地时间每日 `03:30:00`
- 默认保留：30 天
- 陌生人识别：不在本版本范围内

`V0.0.2` 的目标发现先合并云设备列表中的重复设备；列表没有匹配时，仅从
Xiaomi Miot 已加载的内存实体索引恢复目标，且云对象仍必须来自已加载会话。

本地纯测试、固定 Home Assistant 镜像和目标 HAOS 证据记录在 `RELEASE.md`。在单独授权
的目标环境验证中，Home Assistant 媒体目录已切换到 NAS NVMe 后端的 NFS 挂载；一条
既有本地样本先完成等大小与摘要一致性迁移，随后真实服务以单条上限返回
`limit_reached`，发现 3 条、选择 1 条、下载 1 条、失败 0、隔离 0、删除 0。

重启后挂载仍为活动状态，Home Assistant 消费路径与 NAS 目标均看到两条 MP4 且没有
临时文件；新样本通过 `ffprobe`，包含视频轨和音频轨。首次每日计划周期、断网重试、
空间不足和保留删除仍未验证，保持 `UNKNOWN`；115 网盘副本尚未配置。
